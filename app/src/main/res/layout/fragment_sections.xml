<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_diary"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="uk.co.threeequals.landscapeconnect.SectionsFragment">

    <ViewFlipper xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/switcher"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent">

        <!-- Intro screen -->
        <include layout="@layout/fragment_sections_page1"/>

        <!-- Sections list screen -->
        <include layout="@layout/fragment_sections_page2"/>

    </ViewFlipper>
</LinearLayout>
